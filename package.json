{
  "name": "hari-healthy-diet-app",
  "version": "1.0.0",
  "description": "Cloudflare Pages friendly daily diet planner app specialized for medical report analysis and daily meal planning.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "test": "vitest run",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.44.0",
    "jsdom": "^24.1.0",
    "vite": "^6.4.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.110.0"
  }
}
